Create a react page component named CustomerFormPage that is a form based UI with the below mentioned sections to capture a customer’s details in customerInfo object and save that to the server, using the given UI specifications after that.

Sections required in the UI
1. Personal information (row layout with 3 columns in each row, padding 4px all around within each row, margin 8px on left and right of each column) - Add section heading at top before section fields, heading type h3
a. title (select, options - [mr, mrs], width 150px)
b. first name (input)
c. middle name (input)
d. last name (input)
e. DOB (date, width 300px)
f. gender (radio button, options - [male, female, others], row layout with each option coming as a column inside along with the label)
g. mobile (input)

2. Address information (row layout with 1 column in each row, padding 4px all around within each row, margin 8px on left and right of each column) - Add section heading at top before section fields, heading type h3
a. house / flat no (input)
b. street (input)
c. landmark (input)
d. city (input)
e. district (input)
f. country (select, options - [india, us, uk, others], width: 200px)
g. pincode (input)

3. Passport information (row layout with 2 columns in each row, padding 4px all around within row, margin 8px on left and right of each column) - Add section heading at top before section fields, heading type h3
a. passport no (input)
b. parent FFN (input)
c. passport issual country (input)
d. passport expiry (dob)

4. Two action buttons in row layout (1 row with 2 columns, but each column has spans 4 grid units)
a. Save (primary, onclick call props method saveDataForCustomer() with the customer info)
b. Cancel (secondary, onclick clear all state data)
