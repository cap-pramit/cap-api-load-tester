// import { selectApplicationDetailsDomain } from '../selectors';

describe('selectApplicationDetailsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
