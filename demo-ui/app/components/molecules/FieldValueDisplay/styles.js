import styled, { css } from 'styled-components';

import {
  CapRow,
} from '@capillarytech/cap-ui-library';

export const StyledCapRow = styled(CapRow)`
  margin: 4px 0;
`;

export default css`
  .app-details-field-label {
    line-height: inherit;
    margin-top: 0.143rem;
  }`;