// import { selectViewApplicationDomain } from '../selectors';

describe('selectViewApplicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
