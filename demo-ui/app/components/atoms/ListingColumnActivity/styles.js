import { css } from 'styled-components';
import * as styledVars from '@capillarytech/cap-ui-library/styled/variables';

const { CAP_SPACE_08 } = styledVars;

export default css`
  .ant-divider {
    margin: ${CAP_SPACE_08} 0px;
  }
`;
